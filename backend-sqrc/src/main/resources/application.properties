# NOTE: Sensitive or environment-specific values (datasource credentials, mail passwords,
# API keys) are kept in `application-local.properties` which is ignored by git.
# The next line will import those properties if the file is present on the classpath.
# This makes local development easier without requiring an active Spring profile.
spring.application.name=backend-sqrc
spring.config.import=optional:classpath:application-local.properties

# Default supervisor used by report endpoints when no auth is present
app.default.supervisorId=1
# URL pública del frontend (usada para construir enlaces en emails)
app.frontend.url=https://modulo-sqrc.up.railway.app
# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SLA defaults (minutes)
# Example: reclamos = 1440 (24h), solicitudes = 2880 (48h)
app.sla.defaultMinutes=1440
app.sla.porTipo.RECLAMO=1440
app.sla.porTipo.SOLICITUD=2880

# Gemini endpoint/model configuration (keep non-secret parts here)
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
gemini.api.model=gemini-2.5-flash

# Database Configuration (local/dev)
spring.datasource.url=jdbc:mysql://db-erp.mysql.database.azure.com:3306/bd_sqrc
spring.datasource.username=modulo-sqrc
spring.datasource.password=sqrc
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP connection pool settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-timeout=30000

# Mail (Gmail / SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=areadeatencionalcliente730@gmail.com
spring.mail.password=vpndkizoozqvpmsk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# API keys
gemini.api.key=AIzaSyDaaRhN7QeifME1vzSyWMuuUlgyzJdKEak

# Supabase
supabase.url=https://qeqaluswtstmqvvqfzvg.supabase.co
supabase.service-key=sb_secret_kNLsaYXt8GuB2g3rcZg0RA_p2uzRqsh
supabase.bucket.manuales=respuestas_manuales
supabase.bucket.automaticas=respuestas_automaticas